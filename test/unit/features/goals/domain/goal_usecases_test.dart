// Tests for GoalUseCases (GetGoalsUseCase, CreateGoalUseCase, DepositUseCase, UpdateGoalUseCase).
// goal_usecases.dart is currently an empty stub.

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GoalUseCases (stub — not yet implemented)', () {
    test('placeholder — GetGoalsUseCase returns goals list from repository', () => expect(true, isTrue));
    test('placeholder — CreateGoalUseCase validates deadline is in the future', () => expect(true, isTrue));
    test('placeholder — DepositUseCase rejects deposit on locked goal', () => expect(true, isTrue));
    test('placeholder — UpdateGoalUseCase normalises period before saving', () => expect(true, isTrue));
  });
}