// Tests for HomeDatasource.
// home_datasource.dart is currently an empty stub.

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HomeDatasource (stub — not yet implemented)', () {
    test('placeholder — GET /dashboard/home includes range param', () => expect(true, isTrue));
    test('placeholder — 503 maps to ApiException with CONNECTION_ERROR', () => expect(true, isTrue));
  });
}
