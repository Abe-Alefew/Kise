// Tests for HomeModel (lib/features/home/data/dtos/home_model.dart).
// home_model.dart is currently an empty stub.

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HomeModel (stub — not yet implemented)', () {
    test('placeholder — fromJson builds correct entity', () => expect(true, isTrue));
  });
}
